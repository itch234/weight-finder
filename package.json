{
  "name": "weight-finder",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "test:browser": "node tests/browser.cjs",
    "test:touch": "node tests/browser.cjs --touch-only"
  }
}
